<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<%- messages() %>
<% if (errors) { %>
  <ul class="notice">
 <% errors.array().forEach(error => { %>
   <li><%= error.msg %></li>
<%  }) %>
 </ul>
<% } %>

<div class="formContainer" >
  <ul class="inv-management">
    <li><a href="/inventory/add-classification">Add New Classification</a></li>
    <li><a href="/inventory/add-inventory">Add New Inventory Item</a></li>
</ul>
</div>